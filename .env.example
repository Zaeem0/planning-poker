# Environment Variables Configuration
# Copy this file to .env.local for local development:
# cp .env.example .env.local

# Server Configuration
# Port the server will run on (Render will set this automatically in production)
PORT=3000

# Hostname for the server
# Use "localhost" for local development, "0.0.0.0" for production
HOSTNAME=localhost

# Node environment (development or production)
NODE_ENV=development

# CORS Configuration
# In production, set this to your frontend URL for security
# Use "*" for development or if frontend and backend are on same domain
CORS_ORIGIN=*

# Socket.IO Configuration
# For local development, leave this empty (defaults to same origin)
# For production on Render, this should be your Render app URL
# Example: https://your-app-name.onrender.com
NEXT_PUBLIC_SOCKET_URL=

